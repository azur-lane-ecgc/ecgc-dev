{
  "name": "ecgc-dev",
  "private": true,
  "workspaces": [
    "packages/dev",
    "packages/frontend",
    "packages/gsheets2img"
  ],
  "scripts": {
    "prepare": "bunx lefthook install",
    "update": "bun upgrade && bun update --latest && bun install",
    "rebuild": "rm -rf node_modules && rm -rf packages/frontend/node_modules && rm -rf packages/dev/node_modules && rm -rf packages/frontend/dist && rm -rf packages/frontend/.astro && rm -rf .venv && bun run update",
    "reset": "bun run rebuild && bun run devtools && bun run build",
    "submodule": "git submodule update --recursive --remote",
    "gsheets2img": "bun --filter gsheets2img main -g",
    "imgur": "bun --filter gsheets2img main -i",
    "gsheets2imgur": "bun --filter gsheets2img main -gi",
    "compress": "bun --filter dev compress",
    "smol-format": "bunx --bun prettier --write \"packages/**/*.{astro,json}\" --ignore-path .prettierignore",
    "format": "bunx --bun prettier --write \"packages/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue,json,css}\" --ignore-path .prettierignore",
    "lint": "bunx --bun oxlint --fix",
    "devtools": "bun run submodule && bun --filter dev main && bun run smol-format",
    "build": "bun --filter frontend build",
    "check": "bun --filter frontend check && bun --filter dev check && bun --filter gsheets2img check",
    "compress-build": "bun run build && bun run compress",
    "dev": "bun --filter frontend dev",
    "cf-dev": "bunx wrangler dev",
    "start": "NODE_ENV=PRODUCTION bun --filter frontend preview",
    "deploy": "bun run compress-build && bunx wrangler deploy"
  },
  "devDependencies": {
    "@prettier/plugin-oxc": "^0.0.4",
    "knip": "^5.63.1",
    "lefthook": "^1.13.1",
    "oxlint": "^1.16.0",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^0.14.1",
    "prettier-plugin-astro-organize-imports": "^0.4.11",
    "prettier-plugin-organize-imports": "^4.3.0",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "typescript": "^5.9.2",
    "wrangler": "^4.38.0"
  }
}
