---
import { getStaticPaths } from "./[year].astro"

const paths = await getStaticPaths()

return Astro.redirect(
  `/ecgc-dev/changelog/${paths[paths.length - 1].params.year}`,
)
---

{false && paths}
